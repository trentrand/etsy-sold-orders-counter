/**
 * This file is an example of the secrets which a user is required to define.
 *
 * Before your Etsy Sold Orders Counter will work, you must copy or rename this
 * file to `./config.h` and replace the placeholders with your secrets.
 *
 * Secrets should not be committed, so make sure you don't change anything in the
 * example. The `./config.h` file is gitignored, so you don't have to worry about
 * accidentally committing that file.
 */
#define WIFI_SSID "Replace with your wifi network name"
#define WIFI_PASS "Replace with your wifi network password"

#define ETSY_USER "Replace with your Etsy shop identifier, found in the shop url"
#define ETSY_API_KEY "Replace with your Etsy API key from openapi.etsy.com"
